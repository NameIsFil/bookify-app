import {
    StyledSearchButton
} from './SearchButton.styled.tsx';
export const SearchButton = () => {
    return (
        <StyledSearchButton>
            Search for venue
        </StyledSearchButton>
    );
};
